import React from 'react';
import {ScrollView, Text, View} from 'react-native';

const PremiumScreen = () => {
  return (
    <ScrollView>
      <View>
        <Text> Aqui é a Tela PremiumScreen</Text>
      </View>
    </ScrollView>
  );
};
export default PremiumScreen;
