import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    padding: 10,
    backgroundColor: '#ccc',
  },
  title: {
    fontSize: 24,
    fontWeight: 'bold',
    color: '#fff',
    textAlign: 'center',
    marginBottom:10,
  },

  paragrafo:{
    textAlign:'justify',
    fontSize: 16,
    lineHeight:23,
    padding: 10,
    color: '#000',
  }

  
});
export default styles;
